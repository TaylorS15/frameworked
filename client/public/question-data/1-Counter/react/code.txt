function App() {
  const [value, setValue] = React.useState(0);
  
  return (
    <div>
      <h1>Counter</h1>
      <button onClick={() => setValue(value + 1)}>Click Me</button>
      <p>{value}</p>
    </div>
  );
}